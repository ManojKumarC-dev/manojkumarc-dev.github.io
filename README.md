# Minimalistic Portfolio Website

A minimal portfolio scaffold built with React, Vite, Tailwind CSS, Framer Motion, and Lenis smooth scrolling.

## Setup

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## GitHub Pages

Update contact links and project content in `src/components/`, then deploy with:

```bash
npm run deploy
```
